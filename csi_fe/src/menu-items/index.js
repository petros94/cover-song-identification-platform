import dashboard from './dashboard';
import model from './model';
import songs from './songs';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, songs, model]
};

export default menuItems;
